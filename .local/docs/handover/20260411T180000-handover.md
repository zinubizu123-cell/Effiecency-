# Handover — claude-mem multi-node v2 stabilisation

## 1. Identification

| Field | Value |
|-------|-------|
| session_id | 20260411T180000 |
| repo | /Users/regis/Development/GitHub/thedotmack/claude-mem |
| branch | feat/multi-node-v2 |
| CLI | Claude Code (Opus 4.6 1M) |
| exit_reason | quality_failure |

## 2. État actuel

### Ce qui FONCTIONNE (vérifié par tests réels)
- Auth middleware monté et fonctionnel (401 sans token, 200 avec)
- Proxy client → serveur connecté (versionMatch=true)
- Admin routes bloquées par le proxy (403)
- Provenance écrite en DB pour les nouvelles observations (node, instance non-null)
- Settings sync périodique client → serveur
- SettingsDefaultsManager inclut les clés réseau (fix critique de cette session)
- Health endpoint renvoie mode + node
- 4 nodes déployés sur feat/multi-node-v2
- LLM source tracking implémenté (détection claude/codex/gemini)

### Ce qui est CASSÉ (régressions non résolues)
1. **MetadataFooter absent sur les observations** — les pills node/platform/instance/llm_source ne s'affichent pas sur les cards. Le composant MetadataFooter existe mais n'est probablement pas importé dans ObservationCard/SummaryCard dans la version buildée
2. **PromptCard sans MetadataFooter** — les prompts n'affichent aucune provenance
3. **Design capsule header** — la capsule v12.1.0/mode/clients fonctionne mais vérifier la parité exacte avec le screenshot original (commit 4c5bba56 de feat/multi-machine-network)
4. **Observations historiques** — les observations créées avant le fix SettingsDefaults ont node=null en DB (irréversible)
5. **Console debug panel** — visible dans l'original, vérifier si c'est une feature upstream ou notre ajout

### Upstream PR #1722
- URL: https://github.com/thedotmack/claude-mem/pull/1722
- État: OPEN, CHANGES_REQUESTED par CodeRabbit
- Force-pushes: multiples (contraire au skill contributing-upstream)
- Bot comments: ~40 répondus, certains encore ouverts
- **Action requise**: évaluer si la PR est sauvable ou si elle doit être fermée et rouverte proprement

### Staging PR
- PR #3 (fork, tracker déploiement): OPEN
- PR #4 (fork, staging v2): CLOSED

## 3. Bugs critiques trouvés cette session

| Bug | Impact | Détecté par |
|-----|--------|-------------|
| SettingsDefaultsManager filtre les clés réseau | Feature multi-node entièrement non-fonctionnelle | Test réel bun -e getNetworkMode() |
| Auth middleware défini mais jamais monté | Serveur exposé sans auth | Codex round 2 |
| Provenance jamais écrite en DB (INSERT sans colonnes) | node/platform/instance toujours null | Codex round 2 |
| App.tsx n'importe pas les composants multi-node | Viewer identique à upstream | Test réel grep viewer-bundle |
| useSSE sans onClientEvent | Client events SSE perdus | Diff vs branche originale |
| viewer-template.html sans styles topology | Topology panel sans CSS | Test réel |
| Health endpoint sans mode/node | Viewer condition toujours false | Test réel curl /api/health |
| Worker daemon tourne depuis le cache plugin, pas le marketplace | Mauvais code exécuté | ps -p $(lsof -ti :37777) |

## 4. Root cause

Le patch v2 (git diff upstream/main..feat/multi-machine-network appliqué sur une branche neuve) a perdu des modifications viewer qui existaient dans la branche originale. Les subagents ont ensuite supprimé des fichiers et introduit des régressions supplémentaires. À aucun moment un test visuel réel n'a été fait pour vérifier le viewer.

## 5. Reprendre

```
1. Comparer CHAQUE fichier src/ui/ entre feat/multi-machine-network (commit 4c5bba56) et feat/multi-node-v2 HEAD
   git diff 4c5bba56..feat/multi-node-v2 -- src/ui/
   Toute différence est une régression potentielle.

2. Vérifier MetadataFooter est importé et rendu dans ObservationCard, SummaryCard, PromptCard
   grep -rn 'MetadataFooter' src/ui/viewer/components/

3. Rebuild + deploy local + ouvrir le viewer dans le browser
   AVANT de faire quoi que ce soit d'autre

4. Comparer visuellement avec le screenshot de référence:
   /Users/regis/Desktop/Capture d'écran 2026-04-10 à 22.42.25.png

5. Seulement quand le viewer est visuellement identique à l'original:
   redéployer sur les 4 nodes + smoke tests

6. Évaluer l'état de la PR upstream #1722 — fermer et rouvrir si trop de force-pushes
```

## 6. AAR

2 AARs créés sur rcr-core-dev PR #13:
- `20260411-160000-fresh-pr-replacement-and-local-polish.md` (6 findings, 5 P1 + 1 P2)
- `20260411-170000-staging-quality-gate-failure.md` (7 findings, 3 P0 + 4 P1)

Les P0:
1. Pas de quality gate avant fermeture staging
2. Agent rationalise le dismiss des findings
3. Pas de redéploiement après fixes staging

## 7. Feedbacks mémorisés

- `feedback_polish_before_staging.md` — review locale obligatoire avant push
- `feedback_staging_zero_changes_requested.md` — jamais fermer staging avec CHANGES_REQUESTED
- `feedback_real_deployment_test.md` — toujours déployer et tester réellement
- `reference_coderabbit_cli.md` — CodeRabbit CLI usage + rate limits Pro
